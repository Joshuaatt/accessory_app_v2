<div class="page-header">
  <h1>Edit <%= @accessory.name %></h1>
</div>

<div class="admin-controls col-xs-12 col-md-6 col-md-offset-3">
  <%= image_tag @accessory.image.url(:large), class: "large-image" %>
  <%= render 'form' %>
</div>
