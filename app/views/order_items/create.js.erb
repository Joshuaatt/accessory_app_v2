<% if @order.errors.any? || @order_item.errors.any? %>
alert("not valid.")
<% else %>






// $("div.cards").click(function() {
//   card = $(this).data("id");
//   $('div[data-id="'+ card +'"]').css("background-color", "red");
// });

  $(".cart-text").html("<%= escape_javascript(render 'layouts/cart_text') %>");
<% end %>
