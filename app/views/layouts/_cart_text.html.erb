
<div class="navbar navbar-fixed-bottom" role="navigation">
  <div class="container-fluid checkout-bar">
    <div class="row">
      <div class="back col-xs-3">
        <%= link_to "Back", :back, class: "btn btn-success back-button" %>
      </div>
      <div class="col-xs-9 cart-text">
        <%= link_to "Continue", cart_path, class: "btn btn-success continue-button" %>
        <span class="total">Total:</span>
        <div class="cart-total">
          <div class="subtotal">
            <%= number_to_currency current_order.subtotal %>
          </div>
          <div class="monthly_subtotal">
            <%= number_to_currency current_order.monthly_subtotal %>
          </div>
        </div>
      </div>
    </div>
    <div class="asterix">
      * Monthly price is an estimate based on 10% APR for 60 months. Speak with dealer representative for price including tax, installation, and other interest rates.
    </div>
  </div>
</div>

<!-- # <%= link_to "#{current_order.order_items.size} Items in Cart ( #{number_to_currency current_order.subtotal} )", cart_path, class: "btn btn-link" %> -->
