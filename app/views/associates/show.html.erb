<div class="page-header">
  <h1>Last step!</h1>
</div>
<%= bootstrap_form_for [@checkout], :url => associate_path, :html => { :multipart => true } do |f| %>
  <%= f.text_field :customer_name %>
  <%= f.text_field :customer_email %>
  <%= f.text_field :customer_phone %>
  <%= f.text_field :stock_number %>
  <%= f.submit "Send" %>
<% end %>


<% current_order.order_items.each do |order_item| %>
  <%= order_item.accessory_id %>
  <%= number_to_currency order_item.total_price %>
  <%= 
<% end %>
